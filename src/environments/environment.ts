// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
	  apiKey: "AIzaSyB4mAXXOwMY8EjW3cPgteFy_W6D8MXbioU",
	  authDomain: "camile-66fac.firebaseapp.com",
	  databaseURL: "https://camile-66fac.firebaseio.com",
	  projectId: "camile-66fac",
	  storageBucket: "camile-66fac.appspot.com",
	  messagingSenderId: "201047455007",
	  appId: "1:201047455007:web:fe8eaec29af86eb1b5ac2a",
	  measurementId: "G-SPVQKY45J2"
  }
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
